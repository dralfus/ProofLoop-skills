interface:
  display_name: "finish-ticket"
  short_description: "Ticket с независимой приёмкой и budget gates"
  default_prompt: "Используй $finish-ticket для выполнения указанного ticket."
policy:
  allow_implicit_invocation: false
